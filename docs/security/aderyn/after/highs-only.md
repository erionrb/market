# Aderyn Analysis Report

This report was generated by [Aderyn](https://github.com/Cyfrin/aderyn), a static analysis tool built by [Cyfrin](https://cyfrin.io), a blockchain security company. This report is not a substitute for manual audit or security review. It should not be relied upon for any purpose other than to assist in the identification of potential security vulnerabilities.
# Table of Contents

- [Summary](#summary)
  - [Files Summary](#files-summary)
  - [Files Details](#files-details)
  - [Issue Summary](#issue-summary)
- [High Issues](#high-issues)
  - [H-1: Contract locks Ether without a withdraw function](#h-1-contract-locks-ether-without-a-withdraw-function)
  - [H-2: Reentrancy: State change after external call](#h-2-reentrancy-state-change-after-external-call)
  - [H-3: Yul block contains `return`](#h-3-yul-block-contains-return)


# Summary

## Files Summary

| Key | Value |
| --- | --- |
| .sol Files | 4 |
| Total nSLOC | 329 |


## Files Details

| Filepath | nSLOC |
| --- | --- |
| src/LendingMarket.sol | 254 |
| src/TransparentUpgradeableProxy.sol | 57 |
| src/interfaces/IERC20.sol | 10 |
| src/interfaces/IPriceOracle.sol | 8 |
| **Total** | **329** |


## Issue Summary

| Category | No. of Issues |
| --- | --- |
| High | 3 |
| Low | 0 |


# High Issues

## H-1: Contract locks Ether without a withdraw function

It appears that the contract includes a payable function to accept Ether but lacks a corresponding function to withdraw it, which leads to the Ether being locked in the contract. To resolve this issue, please implement a public or external function that allows for the withdrawal of Ether from the contract.

<details><summary>1 Found Instances</summary>


- Found in src/TransparentUpgradeableProxy.sol [Line: 11](../../../../src/TransparentUpgradeableProxy.sol#L11)

	```solidity
	contract TransparentUpgradeableProxy {
	```

</details>



## H-2: Reentrancy: State change after external call

Changing state after an external call can lead to re-entrancy attacks.Use the checks-effects-interactions pattern to avoid this issue.

<details><summary>13 Found Instances</summary>


- Found in src/LendingMarket.sol [Line: 170](../../../../src/LendingMarket.sol#L170)

	State is changed at: `supplyPrincipal[msg.sender] += principal`, `totalSupplyPrincipal += principal`
	```solidity
	        uint256 balanceBefore = baseToken.balanceOf(address(this));
	```

- Found in src/LendingMarket.sol [Line: 171](../../../../src/LendingMarket.sol#L171)

	State is changed at: `supplyPrincipal[msg.sender] += principal`, `totalSupplyPrincipal += principal`
	```solidity
	        baseToken.transferFrom(msg.sender, address(this), amount);
	```

- Found in src/LendingMarket.sol [Line: 172](../../../../src/LendingMarket.sol#L172)

	State is changed at: `supplyPrincipal[msg.sender] += principal`, `totalSupplyPrincipal += principal`
	```solidity
	        uint256 transferedAmount = baseToken.balanceOf(address(this)) - balanceBefore;
	```

- Found in src/LendingMarket.sol [Line: 205](../../../../src/LendingMarket.sol#L205)

	State is changed at: `collateralBalance[msg.sender] += transferedAmount`, `totalCollateral += transferedAmount`
	```solidity
	        uint256 balanceBefore = collateralToken.balanceOf(address(this));
	```

- Found in src/LendingMarket.sol [Line: 206](../../../../src/LendingMarket.sol#L206)

	State is changed at: `collateralBalance[msg.sender] += transferedAmount`, `totalCollateral += transferedAmount`
	```solidity
	        collateralToken.transferFrom(msg.sender, address(this), amount);
	```

- Found in src/LendingMarket.sol [Line: 207](../../../../src/LendingMarket.sol#L207)

	State is changed at: `collateralBalance[msg.sender] += transferedAmount`, `totalCollateral += transferedAmount`
	```solidity
	        uint256 transferedAmount = collateralToken.balanceOf(address(this)) - balanceBefore;
	```

- Found in src/LendingMarket.sol [Line: 253](../../../../src/LendingMarket.sol#L253)

	State is changed at: `borrowPrincipal[msg.sender] -= principal`, `totalBorrowPrincipal -= principal`
	```solidity
	        uint256 balanceBefore = baseToken.balanceOf(address(this));
	```

- Found in src/LendingMarket.sol [Line: 254](../../../../src/LendingMarket.sol#L254)

	State is changed at: `borrowPrincipal[msg.sender] -= principal`, `totalBorrowPrincipal -= principal`
	```solidity
	        baseToken.transferFrom(msg.sender, address(this), amount);
	```

- Found in src/LendingMarket.sol [Line: 255](../../../../src/LendingMarket.sol#L255)

	State is changed at: `borrowPrincipal[msg.sender] -= principal`, `totalBorrowPrincipal -= principal`
	```solidity
	        uint256 transferedAmount = baseToken.balanceOf(address(this)) - balanceBefore;
	```

- Found in src/LendingMarket.sol [Line: 286](../../../../src/LendingMarket.sol#L286)

	State is changed at: `borrowPrincipal[borrower] -= principal`, `totalBorrowPrincipal -= principal`, `collateralBalance[borrower] -= seizeAmount`, `totalCollateral -= seizeAmount`
	```solidity
	        uint256 balanceBefore = baseToken.balanceOf(address(this));
	```

- Found in src/LendingMarket.sol [Line: 287](../../../../src/LendingMarket.sol#L287)

	State is changed at: `borrowPrincipal[borrower] -= principal`, `totalBorrowPrincipal -= principal`, `collateralBalance[borrower] -= seizeAmount`, `totalCollateral -= seizeAmount`
	```solidity
	        baseToken.transferFrom(msg.sender, address(this), repayAmount);
	```

- Found in src/LendingMarket.sol [Line: 288](../../../../src/LendingMarket.sol#L288)

	State is changed at: `borrowPrincipal[borrower] -= principal`, `totalBorrowPrincipal -= principal`, `collateralBalance[borrower] -= seizeAmount`, `totalCollateral -= seizeAmount`
	```solidity
	        uint256 transferedAmount = baseToken.balanceOf(address(this)) - balanceBefore;
	```

- Found in src/LendingMarket.sol [Line: 408](../../../../src/LendingMarket.sol#L408)

	State is changed at: `totalReserves -= amount`
	```solidity
	        require(amount <= baseToken.balanceOf(address(this)), "insufficient liquidity");
	```

</details>



## H-3: Yul block contains `return`

This causes the transaction execution to halt, and nothing after that call will execute including code following the assembly block.

<details><summary>1 Found Instances</summary>


- Found in src/TransparentUpgradeableProxy.sol [Line: 58](../../../../src/TransparentUpgradeableProxy.sol#L58)

	```solidity
	            default { return(0, returndatasize()) }
	```

</details>



