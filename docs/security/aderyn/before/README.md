# Aderyn commands used

```sh
forge clean

forge build --force

aderyn . -x test,mocks,lib -o docs/security/aderyn/before/full.md

aderyn . -x test,mocks,lib -o docs/security/aderyn/before/full.json

aderyn . -x test,mocks,lib --highs-only -o docs/security/aderyn/before/highs-only.md
```

```sh
forge clean

forge build --force

aderyn . -x test,mocks,lib -o docs/security/aderyn/after/full.md

aderyn . -x test,mocks,lib -o docs/security/aderyn/after/full.json

aderyn . -x test,mocks,lib --highs-only -o docs/security/aderyn/after/highs-only.md
```

